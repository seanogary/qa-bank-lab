export const api = {
    createAccount: (name, initialDeposit) => {
        name;
        initialDeposit;
    },

    getAccount: (accountId) => {
        accountId;
    },

    deleteAccount: (accountId) => {
        accountId;
    },

    deposit: (accountId, amount) => {
        accountId;
        amount;
    },

    withdraw: (accountId, amount) => {
        accountId;
        amount;
    },

    sendSnail: (fromAccountId, toAccountId, amount) => {
        fromAccountId;
        toAccountId;
        amount;
    },

    getLedger: (accountId) => {
        accountId;
    },

}